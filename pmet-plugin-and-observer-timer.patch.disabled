diff --git a/vendor/magento/framework/Console/Cli.php b/vendor/magento/framework/Console/Cli.php
index 7fae09c..b91cb61 100644
--- a/vendor/magento/framework/Console/Cli.php
+++ b/vendor/magento/framework/Console/Cli.php
@@ -103,6 +103,16 @@ class Cli extends Console\Application
     {
         $exitCode = parent::doRun($input, $output);
 
+
+        if (isset($_SERVER['pluginMethodExecTimes'])){
+            asort($_SERVER['pluginMethodExecTimes']);
+            var_dump($_SERVER['pluginMethodExecTimes']);
+        }
+        if (isset($_SERVER['observerExecTimes'])){
+            asort($_SERVER['observerExecTimes']);
+            var_dump($_SERVER['observerExecTimes']);
+        }
+
         if ($this->initException) {
             throw $this->initException;
         }
diff --git a/vendor/magento/framework/Event/Invoker/InvokerDefault.php b/vendor/magento/framework/Event/Invoker/InvokerDefault.php
index a7a387b..035db01 100644
--- a/vendor/magento/framework/Event/Invoker/InvokerDefault.php
+++ b/vendor/magento/framework/Event/Invoker/InvokerDefault.php
@@ -69,7 +69,20 @@ class InvokerDefault implements \Magento\Framework\Event\InvokerInterface
     protected function _callObserverMethod($object, $observer)
     {
         if ($object instanceof \Magento\Framework\Event\ObserverInterface) {
+
+            $startTime = microtime(true);
+            
             $object->execute($observer);
+            
+            $curOject = 'Event: ' . $observer->get('_data')['event']->get('_data')['name'] . ', class: ' . get_class($object);
+            if (!array_key_exists('observerExecTimes', $_SERVER)) {
+                $_SERVER['observerExecTimes'] = [];
+            }
+            if (!array_key_exists($curOject, $_SERVER['observerExecTimes'])) {
+                $_SERVER['observerExecTimes'][$curOject] = 0;
+            }
+            $_SERVER['observerExecTimes'][$curOject] += (microtime(true) - $startTime);
+
         } elseif ($this->_appState->getMode() == \Magento\Framework\App\State::MODE_DEVELOPER) {
             throw new \LogicException(
                 sprintf(
diff --git a/vendor/magento/framework/Interception/Interceptor.php b/vendor/magento/framework/Interception/Interceptor.php
index 07600c5..3e107f2 100644
--- a/vendor/magento/framework/Interception/Interceptor.php
+++ b/vendor/magento/framework/Interception/Interceptor.php
@@ -109,6 +109,8 @@ trait Interceptor
             $pluginList,
             &$next
         ) {
+            $startTime = microtime(true);
+
             $capMethod = ucfirst($method);
             $currentPluginInfo = $pluginInfo;
             $result = null;
@@ -147,6 +149,17 @@ trait Interceptor
                 }
             }
 
+            if (isset($pluginMethod)) {
+                $curPluginMethod = 'Plugin: ' . $code . ', method: ' . get_class($pluginInstance) . '->' . $pluginMethod;
+                if (!array_key_exists('pluginMethodExecTimes', $_SERVER)) {
+                    $_SERVER['pluginMethodExecTimes'] = [];
+                }
+                if (!array_key_exists($curPluginMethod, $_SERVER['pluginMethodExecTimes'])) {
+                    $_SERVER['pluginMethodExecTimes'][$curPluginMethod] = 0;
+                }
+                $_SERVER['pluginMethodExecTimes'][$curPluginMethod] += (microtime(true) - $startTime);
+            }
+            
             return $result;
         };
 
